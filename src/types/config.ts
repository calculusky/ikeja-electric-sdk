export interface IkejaElectricOptions {
    /** Identifier allocated to client from server side */
    appId: string;
    /** password assigned by server  */
    password: string;
    /** SFTP Username assigned from server */
    sftpUsername: string;
    /** SFTP password assigned from server */
    sftpPassword: string;
}
